/* import express from 'express';
import { manualSync, manualPush } from '../controller/syncController.js';

const router = express.Router();

router.post('/sync', manualSync);
router.post('/push', manualPush);

export default router;
 */